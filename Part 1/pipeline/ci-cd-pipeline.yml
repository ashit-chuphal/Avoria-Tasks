# Candidate should fix structure, security, AWS auth, Docker build, ECR push, and deployment logic.

name: Deploy Interview App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials - bad example
        with:
          export AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
          export AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
          export AWS_DEFAULT_REGION=eu-central-1

        run: |
          export AWS_ACCESS_KEY_ID=AKIA_FAKE_HARDCODED_KEY
          export AWS_SECRET_ACCESS_KEY=FAKE_SECRET_DO_NOT_USE
          export AWS_DEFAULT_REGION=eu-central-1

      - name: Build Docker image
        run: |
          docker build -t interview-app .

      - name: Push image to ECR
        run: |
          docker tag interview-app:latest 123456789012.dkr.ecr.eu-central-1.amazonaws.com/interview-app:latest
          docker push 123456789012.dkr.ecr.eu-central-1.amazonaws.com/interview-app:latest

      - name: Deploy
        run: |
          echo "Deploying..."
          # Missing real deployment command and health check
